---
layout: base
title: Seznam receptů
---

<ul>
    {% for rec in site.menu %}
        <li>
            <a href="{{ site.url }}/{{ rec.url }}">{{ rec.name }}</a>
        </li>
    {% endfor %}
</ul>
